<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @course.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @course.description %>
</p>

<p>
  <strong>Prereqs: </strong>
  <%= @course.all_prereqs.pluck(:title).join(", ") if @course.has_children? %>
</p>

<p>
  <strong>Relevant AOCs: </strong>
  <%= @course.all_majors_as_string %>
</p>

<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
